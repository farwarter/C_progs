#include <stdio.h>

int main()
{
    int n; //1<n<12
    int f = 0;
    scanf("%d", &n);
    if (n > 0)
        f = 1;
    for (int i = 2; i <=n; i++)
                f *= i;
    printf("%d", f);
    return 0;
}
