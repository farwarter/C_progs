//Программа поиска факториала введенного числа (1<n<12).

#include <stdio.h>

int main()
{
    int n; 
    int f = 0;
    scanf("%d", &n);
    if (n > 0)
        f = 1;
    for (int i = 2; i <=n; i++)
                f *= i;
    printf("%d", f);
    return 0;
}
